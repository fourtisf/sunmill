/**
 * The canonical home, in one place.
 *
 * This is displayed inside the game on purpose. A wallet-connect product with
 * a near-miss domain available to anyone is a phishing target, and the only
 * defence a player has is knowing the real address — which has to come from
 * inside the product, not from a post that can be faked just as easily.
 */
export const SITE_URL = 'https://sunmil.fun';
export const SITE_DOMAIN = 'sunmil.fun';
