{
  "name": "sunmil",
  "private": true,
  "version": "1.0.0",
  "description": "SUNMIL — server-authoritative production-chain farm game",
  "workspaces": [
    "server",
    "frontend"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=frontend",
    "build": "npm run build --workspace=server && npm run build --workspace=frontend",
    "test": "npm run test --workspace=server",
    "typecheck": "npm run typecheck --workspace=server && npm run typecheck --workspace=frontend",
    "prisma:generate": "npm run prisma:generate --workspace=server",
    "prisma:migrate": "npm run prisma:migrate --workspace=server",
    "prisma:deploy": "npm run prisma:deploy --workspace=server",
    "seed": "npm run seed --workspace=server",
    "start": "pm2 start ecosystem.config.js"
  }
}
